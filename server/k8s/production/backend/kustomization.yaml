apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: backend-kustomization
  namespace: production

namespace: production

resources:
    - deployment.yaml
    - configmap.yaml
    - service.yaml  
    - ingress.yaml
